# Declutr Agent Planning Engine Guide

The `PlanningEngine` decomposes persistent user goals into structured multi-step execution plans (`AgentPlan`) containing sequence indices, tool parameters, confidence ratings, and approval checkpoints.
